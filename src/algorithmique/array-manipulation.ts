/**
 * Array manipulations for algorithm definition
 */


/**
 * 1. Compute the sum of the numbers of intArray
 */
export const sum = (intArray: number[]): number => {
    return 0;
}

/**
 * 2. Compute the average from intArray
 */
export const average = (intArray: number[]): number => {
    return 0;
}

/**
 * 3. Find the max number of intArray
 */
export const max = (intArray: number[]): number => {
    return 0;
}

/**
 * 4. Find the min number of intArray
 */
export const min = (intArray: number[]): number => {
    return 0;
}

/**
 * 5. Create an array that contains only odd numbers from intArray
 */
export const odds = (intArray: number[]): number[] => {
    return [];
}

/**
 * 6. Produce a unique values from intArray
 */
export const uniqueValues = (intArray: number[]): number[] => {
    return [];
}